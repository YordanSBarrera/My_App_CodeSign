import Formulario from "../Formulario";

const AdjacentElementsProduct = () => {
  /*Given an array of integers, find the pair of adjacent elements that has the largest product and return that product.
    Example
    For inputArray = [3, 6, -2, -5, 7, 3], the output should be
    adjacentElementsProduct(inputArray) = 21.
    7 and 3 produce the largest product.*/
  /* const respuesta = () => {
    return "VACIO x AHORA";
  };
  const capturaVariable = (valor) => {
    const a = valor + " Esta es la Respuesta!!";
    return a;
  };*/
  /*
   itemForm.NameFormulario... nombre del formulario
   itemForm.ValorInicial
   itemForm.funcion  
   itemForm.NameBotton

   <Formulario />
  */
  return <div>/*Formulario = (formName, formText, fuResp*/</div>;
};

export default AdjacentElementsProduct;
